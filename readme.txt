# Disaster tweets using kaggle's dataset training on text data as csv labeled as disaster or not.
# Using the keras API to preprocess the data and also make predictions using different models
# starting from non deep learning models like naive bayes to deep learning models using RNNS and convolution. 